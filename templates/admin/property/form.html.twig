{{ form_start(form) }}
<div class="row">
    <div class="col-md-4">{{ form_row(form.titel) }}</div>
    <div class="col-md-4">{{ form_row(form.surface) }}</div>
    <div class="col-md-4">{{ form_row(form.price) }}</div>
</div>
<div class="row">
    <div class="col-md-2">{{ form_row(form.rooms) }}</div>
    <div class="col-md-2">{{ form_row(form.bedroo) }}</div>
    <div class="col-md-2">{{ form_row(form.heat) }}</div>
    <div class="col-md-">{{ form_row(form.floor) }}</div>
</div>
<div class="row">
    <div class="col-md-7">{{ form_row(form.imageFile) }}</div>
    <div class="col-md-5">{{ form_row(form.adress) }}</div>
    <div class="col-md-5">{{ form_row(form.city) }}</div>
    <div class="col-md-5">{{ form_row(form.postal_code) }}</div>
    <div class="col-md-5">{{ form_row(form.sold) }}</div>
</div>

{{ form_widget(form) }}
<button class="btn btn-primary">{{ buton|default('Enregistrer') }} </button>